{{ form_start(formule) }}
    <div class="row">
        <div class="col-md-6">
            {{ form_row(formule.nom) }}
            {{ form_row(formule.prenom) }}
            {{ form_row(formule.email) }}
        </div>
        <div class="col-md-6">
            {{ form_row(formule.adresse) }}
            {{ form_row(formule.Password) }}
        </div>

        <div class="col-md-6">
        </div>
        
        <div class="text-center mt-4">
            {{ form_row(formule.submit, { 'label': 'Inscription','attr': {'class': 'btn btn-primary btn-sm'} }) }}
        </div>
    </div>
{{ form_end(formule) }}